# Core (required) — offline formatter
python-docx>=1.1.0
pdfplumber>=0.11.0
pypdf>=4.0.0

# Web app (optional, for the browser UI)
streamlit>=1.30.0

# AI enhancement (optional, bring your own key) — install only if you want --enhance
# openai>=1.0.0
# google-generativeai>=0.5.0
